# Backend dependencies
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.25.0

# LLM Gateway (provider-neutral, optional): chi duoc import khi AI_API_KEY
# duoc cau hinh (xem app/services/llm/gateway.py, D-011). Thieu package nay
# van chay duoc, gateway se fallback deterministic offline.
openai>=1.40.0

# RAG semantic layer (optional, xem app/services/rag/vector_index.py, D-013).
# Chi duoc import khi RAG_SEMANTIC_MODE=hybrid. Thieu 2 package nay van chay
# duoc, retrieval se tu dong fail-closed ve lexical-only (hanh vi mac dinh
# khong doi). Luu y: sentence-transformers keo theo torch (~vai tram MB-GB),
# lan dau bat hybrid can network de download model tu HuggingFace Hub.
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0

# Testing and development
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
